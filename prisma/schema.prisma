generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Destaque {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  date        String
  type        String   // 'news' ou 'event'
}
